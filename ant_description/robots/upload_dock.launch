<launch>
  <arg name="dock" default="full"/>

  <!-- Dock description -->
  <param name="dock_description" command="$(find xacro)/xacro.py '$(find ant_description)/robots/dock_$(arg dock).urdf.xacro'" />
</launch>
